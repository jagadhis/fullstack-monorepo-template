pre-commit:
  parallel: true
  commands:
    lint:
      glob: "apps/frontend/**/*.{js,ts,jsx,tsx}"
      run: npx turbo run lint --filter=frontend

commit-msg:
  commands:
    lint-commit-msg:
      run: npx commitlint --edit {1}